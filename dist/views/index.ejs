<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title></title>
</head>
<body>
  <h1><%= title %></h1>
  <ul>
    <% messages.map((message) => { %>
      <li>
        <%=`${message.username}: ${message.message}`%>
        <form action="/message/<%= message.id %>" method="GET">
          <button type="submit">Open</button>
        </form>
      </li>
    <% }); %>
  </ul>
  <form action="/new" method="GET">
    <button type="submit">Create new message</button>
  </form>
</body>
</html>
